[package]
name = "kerotakis-stack"
description = "The one standard solver stack for Kerotakis hosts — CLI, shell and wasm assemble the same equilibrator order from here, so transports cannot drift"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
kerotakis-core = { path = "../kerotakis-core" }
kerotakis-cea = { path = "../kerotakis-cea" }
# BRD-020: the reaction-family router rides on kerotakis-org's structural
# oracle; the stack owns its position, org owns the chemistry.
kerotakis-org = { path = "../kerotakis-org" }
