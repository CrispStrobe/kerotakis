[package]
name = "kerotakis-cli"
description = "Kerotakis virtual laboratory — REPL, batch runner and JSON interface"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "kero"
path = "src/main.rs"

[dependencies]
kerotakis-core = { path = "../kerotakis-core" }
kerotakis-thermo = { path = "../kerotakis-thermo" }
kerotakis-phreeqc = { path = "../kerotakis-phreeqc" }
kerotakis-cea = { path = "../kerotakis-cea" }
kerotakis-stack = { path = "../kerotakis-stack" }
kerotakis-codex = { path = "../kerotakis-codex" }
kerotakis-data = { path = "../kerotakis-data" }
kerotakis-org = { path = "../kerotakis-org" }
csv = { workspace = true }
rayon = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
kerotakis-safety = { path = "../kerotakis-safety" }
postcard = { version = "1", features = ["alloc"] }
sha2 = "0.10"
toml = "0.8"

[dev-dependencies]
postcard = { version = "1", features = ["alloc"] }
