# Corpus perturbation generator (work in progress)

Placeholder for the draft pull request. The corpus at `tests/coverage/curiosity-v1/`
records a ROUTE in `expected`, not an outcome, so it asserts nothing about the
chemistry. The scripts beside it are real experiments. This branch turns them into
perturbation cases.
