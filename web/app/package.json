{
  "name": "kerotakis-app",
  "private": true,
  "version": "0.0.1",
  "description": "The Kerotakis bench UI \u2014 one web-standard app for PWA and Tauri shells (ROADMAP-GUI.md)",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "licences": "node tools/licence-lint.mjs",
    "payload": "../../tools/build-shell-payload.sh",
    "build:shell": "npm run payload && vite build",
    "dev:shell": "npm run payload && vite",
    "tauri": "tauri"
  },
  "dependencies": {
    "svelte": "^5.38.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.0",
    "@tauri-apps/cli": "^2.11.4",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
