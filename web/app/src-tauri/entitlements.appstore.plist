<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The Mac App Store variant of entitlements.plist, used only by
         tauri.macos-appstore.conf.json. Xcode injects the two identity
         keys below on its own; Tauri codesigns directly and does not, and
         without them the App Store provisioning profile does not match
         what is being signed. They are also exactly why this is a separate
         file: a build signed with anything other than that profile is
         rejected outright for naming an identifier it cannot claim. -->
    <key>com.apple.application-identifier</key>
    <string>N9XSJ4M3GT.com.crispstrobe.kerotakis</string>
    <key>com.apple.developer.team-identifier</key>
    <string>N9XSJ4M3GT</string>

    <!-- The rest is entitlements.plist verbatim: the sandbox the store
         requires, the open/save panel, and printing. No network. -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.print</key>
    <true/>
</dict>
</plist>
