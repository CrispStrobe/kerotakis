{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may call the engine command; nothing else.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
